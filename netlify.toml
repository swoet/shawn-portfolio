# Admin site build config (monorepo)
# Build only the admin-dashboard app
[build]
  command = "cd admin-dashboard && npm install --no-audit --no-fund && npm run build"
  publish = "admin-dashboard/.next"

[build.environment]
  NODE_VERSION = "20"
